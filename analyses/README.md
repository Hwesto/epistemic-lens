# Daily framing analyses

Auto-generated by `.github/workflows/daily.yml` after ingest:

  ingest → extract → dedup → build_briefing → build_metrics → claude analysis

Each `<DATE>_<story_key>.md` follows the structure of
`docs/HORMUZ_CORRELATION.md` exactly. The analysis is the canonical
analytical product — videos, carousels, and posts derive from it.

Numbers come from `briefings/<DATE>_<story_key>_metrics.json` (precomputed
by `build_metrics.py`). The Claude analysis pass uses them verbatim.

Prompt: `.claude/prompts/daily_analysis.md`.
