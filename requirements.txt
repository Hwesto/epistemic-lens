# Python >=3.12 required (matches CI; some modules use 3.10+ syntax features).
#
# Core daily-pipeline deps only. The video tree (TTS providers,
# audio I/O) lives in requirements-video.txt — install separately
# if you're running the manual / optional video pipeline.
#
# Stage 21 (Gap 21-1): semver upper bounds prevent accidental major
# drift in analytical computation (DBSCAN tie-breaks, embedding model
# tokenization, jsonschema strictness). For full reproducibility,
# generate a pip-compile lock and hash it in meta_version.json
# (backlog item — not yet implemented).

requests>=2.28,<3.0
trafilatura>=2.0,<3.0
sentence-transformers>=2.2.0,<5.0
numpy>=1.24.0,<3.0
scikit-learn>=1.3.0,<2.0
jsonschema>=4.0,<5.0      # fail-closed schema validation in renderers + validate_analysis

# feedparser no longer required (v0.4 uses xml.etree directly)
# argostranslate no longer required (v0.2 had translation; v0.4 relies on multilingual embeddings)
